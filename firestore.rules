rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{telegramId} {
      allow read: if true;
      allow create, update: if request.resource.data.telegramId == int(telegramId);
      allow delete: if false;
    }
  }
}
